# This is a generated Android Gradle configuration placeholder for Flutter 3.32+.
# The official Flutter project template should be regenerated locally with:
# flutter create . --platforms=android --project-name pulse --org com.pulse
# Then verify android/app/build.gradle(.kts) targets compileSdk/targetSdk 36+.
